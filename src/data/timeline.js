// Milestones for the /timeline page, oldest first. Each entry needs a year, a
// title and a short body, plus an optional `image` (path under public/, e.g.
// '/assets/timeline/2019-first-app.jpg') to show a photo alongside it. Add one
// whenever there's a new chapter worth marking.
export const TIMELINE = [];
