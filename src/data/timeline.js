// Milestones for the /timeline page, oldest first. Each entry needs a year, a
// title and a short body, plus an optional `image` to show photos on hover —
// a single path under public/ (e.g. '/assets/timeline/2019-first-app.jpg') or
// an array of paths (e.g. ['/assets/timeline/a.jpg', '/assets/timeline/b.jpg'])
// to show more than one. Add an entry whenever there's a new chapter worth marking.
export const TIMELINE = [];
